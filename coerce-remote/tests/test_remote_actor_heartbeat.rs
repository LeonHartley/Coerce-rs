#[macro_use]
extern crate coerce_macros;

#[coerce_test]
pub async fn test_remote_actor_heartbeat() {

    // Create an actor
    // Ensure actor sends heartbeat
    // If actor doesn't send heartbeat within specific time, it will be noted as `Dead`
    //
}
