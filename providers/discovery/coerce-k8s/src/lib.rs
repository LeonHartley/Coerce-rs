///
/// TODO: Create actors that can watch the k8s API, discovering coerce cluster nodes
///       based on a configured pod-selection criteria
///

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
